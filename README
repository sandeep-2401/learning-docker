This repository contains a Docker-ready Node.js + TypeScript backend that demonstrates how to build, containerize, and run a modern API using Docker and Docker Compose.

The goal of this project was to understand containerization, environment isolation, and reproducible deployments for backend applications.